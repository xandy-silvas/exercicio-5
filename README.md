5. Contagem regressiva

   Escreva uma função recursiva em C que imprima uma contagem regressiva de `n` até 0.  

   Exemplo:

   Entrada: `3` → Saída: `3 2 1 0`.  

   Restrição: A função deve imprimir os números, não retornar um array.
